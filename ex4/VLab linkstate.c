Link State Routing Algorithm
Choose difficulty:
How often do nodes broadcast Link-State Packets?
b: At regular intervals or when the network changes
Which of the following best describes the forwarding table in Link-State Routing?
b: It maps each destination to the next-hop node on the shortest path
What is the primary advantage of Link-State Routing over Distance Vector Routing?
b: Faster convergence and no count-to-infinity problem
How does a node update D(v) for a neighbor v in the Link-State algorithm?
c: D(v) = min(D(v), D(w) + c(w,v))
What is the final output of the Link-State algorithm?
a: A complete routing table containing next-hop nodes
